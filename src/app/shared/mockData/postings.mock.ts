import { PostingPayload } from 'src/app/state/posting/posting.state';

export const mockPostingPayload: PostingPayload = {
	userEmail: 'fakeEmail',
	deviceKey: 'fakeKey',
	comments: 'fakeComments',
	price: 99,
};
